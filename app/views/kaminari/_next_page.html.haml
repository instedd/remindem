%span{:class => "button next #{'disabled' if current_page.last?}"}
  = link_to_unless(current_page.last?, raw(t 'views.pagination.next'), url, :rel => 'next', :remote => remote) do
    = link_to raw(tag(:span)), '#'
